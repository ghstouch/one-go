version: '3.8'

services:
  omniroute:
    build: .
    container_name: omniroute
    ports:
      - "8080:8080"
    volumes:
      - omniroute-data:/app/storage
    environment:
      - GIN_MODE=release
      - SERVER_PORT=8080
      - SERVER_HOST=0.0.0.0
      - DB_DRIVER=sqlite
      - DB_SQLITE_PATH=/app/storage/omniroute.db
      - JWT_SECRET=change-me-in-production
      - LOG_LEVEL=info
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost:8080/api/health"]
      interval: 30s
      timeout: 5s
      retries: 3

volumes:
  omniroute-data:
